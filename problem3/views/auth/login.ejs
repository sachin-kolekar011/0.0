<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        /* General Styling */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
        }

        .container {
            max-width: 400px;
            margin: 50px auto;
            padding: 20px;
            background: white;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            text-align: center;
        }

        h1 {
            margin-bottom: 20px;
            font-size: 2rem;
            color: #333;
        }

        .form-group {
            margin-bottom: 15px;
            text-align: left;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            color: #666;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .flash-error {
            color: #d9534f;
            margin-bottom: 15px;
        }

        .btn {
            padding: 10px 15px;
            background-color: #2a9d8f;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
        }

        .btn:hover {
            background-color: #21867a;
        }

        p {
            margin-top: 15px;
            color: #333;
        }

        a {
            color: #2a9d8f;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        /* Flash Message */
        .flash-error {
            color: #d9534f;
            font-size: 1rem;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Login</h1>
        
        <!-- Flash message for errors -->
        <div class="flash-error">
            <%= error ? error : '' %> <!-- Display error if exists -->
        </div>
        
        <!-- Login Form -->
        <form action="/login" method="POST" class="form-login">
            <div class="form-group">
                <label for="username">Username:</label>
                <input type="text" id="username" name="username" required placeholder="Enter your username">
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required placeholder="Enter your password">
            </div>
            <button type="submit" class="btn">Login</button>
        </form>
        
        <!-- Register Link -->
        <p>Don't have an account? <a href="/signup">Register here</a>.</p>
    </div>

    <script>
        // Any JavaScript code can go here if needed, such as validation
    </script>

</body>
</html>
