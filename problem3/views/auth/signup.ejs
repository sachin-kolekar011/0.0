<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StudentHelp</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: linear-gradient(135deg, #8EC5FC, #E0C3FC);
            color: #333;
        }

        .signup {
            background: #fff;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 400px;
            text-align: center;
        }
        .signup h1 {
    font-size: 2rem;
    color: #4A90E2;
    margin-bottom: 10px;
}
.signup h3 {
    margin-bottom: 20px;
    font-weight: bold;
    color: #555;
}
.signup input {
    width: 100%;
    padding: 10px;
    margin: 10px 0;
    border: 1px solid #ccc;
    border-radius: 8px;
    font-size: 1rem;
    box-sizing: border-box;
}
.signup input:focus {
    border-color: #4A90E2;
    outline: none;
    box-shadow: 0 0 8px rgba(74, 144, 226, 0.2);
}
.signup button {
    width: 100%;
    padding: 12px;
    background-color: #4A90E2;
    color: white;
    font-size: 1rem;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

.signup button:hover {
    background-color: #357ABD;
}
.flash-message {
            background-color: #f8d7da;
            color: #721c24;
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 5px;
            text-align: center;
            font-weight: bold;
        }

    </style>
     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
</head>

<body>
    <div class="signup">
        <% if (error && error.length > 0) { %>
            <div class="flash-message">
                <%= error %>
                <i class="fa-solid fa-xmark" onclick="dismissFlash()" style="padding-left: 10px; cursor: pointer;"></i>
            </div>
        <% } %>
        <% if (success && success.length > 0) { %>
            <div class="flash-message">
                <%= success %>
                <i class="fa-solid fa-xmark" onclick="dismissFlash()" style="padding-left: 10px; cursor: pointer;"></i>
            </div>
        <%}%>
        <h1>StudentHelp</h1>
        <h3>Create your Account</h3>
        <form action="/signup" method="POST">
            <input type="text" placeholder="Username" name="username" required>
            <input type="email" placeholder="@gmail.com" name="email" required>
            <input type="password" placeholder="password" name="password" required>
            <button>SignUp</button>
        </form>

    </div>

    <script>
        function dismissFlash() {
            const flashMessage = document.querySelector('.flash-message');
            flashMessage.style.display = 'none'; // Hide the flash message when "Wrong" is clicked
        }
    </script>

</body>

</html>